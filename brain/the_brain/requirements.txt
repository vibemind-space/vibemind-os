# ============================================================================
# Tahlamus Brain System - Standard Dependencies
# ============================================================================
# Install with: pip install -r requirements.txt
# For minimal install (no web/API): pip install -r requirements-minimal.txt
# For full ML features: pip install -r requirements-full.txt
# ============================================================================

# VibeMind Shared LLM Config
vibemind-shared>=0.1.0

# Core Scientific Computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
scikit-learn>=1.0.0

# Configuration & Utilities
pyyaml>=5.4.0
python-dotenv>=0.19.0

# Testing
pytest>=7.0.0

# Web Framework & API
flask>=2.0.0
flask-cors>=3.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0
jinja2>=3.1.0

# HTTP Client
requests>=2.26.0

# Qdrant vector DB client — Phase 11.U.K (2026-06-02). qdrant_kg.py + 7 downstream
# subsystems (ConsolidationEngine, SnapshotEngine, DiscourseEngine, DiscourseAggregator,
# MirofishKGSync, SelfAwarenessWatcher, DiscourseMemoryConsolidator) all depend on
# this. Was missing -> Brain logs "QdrantKG unavailable: No module named 'qdrant_client'"
# at every boot. Knowledge Graph + Events + Self-Model tabs stayed empty as a result.
qdrant-client>=1.7.0

# Cloud Storage (S3-compatible)
boto3>=1.26.0

# LLM & AI Integration
openai>=1.0.0
anthropic>=0.3.0

# AutoGen for Swarm (requires specific versions)
pyautogen>=0.2.0

# Optional: Jupyter (for notebooks - if you add any)
notebook>=6.4.0

# Optional: PyTorch (for deep learning features)
# Uncomment if you need CTM training or neural features:
# torch>=1.10.0
# torchvision>=0.11.0

# Web Search (ResearchAgent)
ddgs>=6.0.0

# Neural Manifold visualization
umap-learn==0.5.11
pynndescent==0.6.0
