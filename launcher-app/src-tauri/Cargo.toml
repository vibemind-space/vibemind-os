[package]
name = "vibemind-launcher"
version = "0.1.0"
description = "Cross-platform launcher for the VibeMind server-mode start/stop scripts."
authors = ["Vibemind"]
license = ""
edition = "2021"
rust-version = "1.77"

[lib]
name = "vibemind_launcher_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
