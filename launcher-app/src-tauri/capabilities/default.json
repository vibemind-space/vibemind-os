{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: invoke the spawn/stop commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
