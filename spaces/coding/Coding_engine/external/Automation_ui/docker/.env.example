# =============================================================================
# TRAE Desktop Streaming - Docker Environment Configuration
# =============================================================================
# Kopieren Sie diese Datei nach .env und passen Sie die Werte an:
#   cp docker/.env.example .env
# =============================================================================

# ============================================
# Port Konfiguration
# ============================================
FRONTEND_PORT=5173
BACKEND_PORT=8007
OCR_PORT=8008
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
REDIS_PORT=6379

# ============================================
# Node/React Konfiguration
# ============================================
NODE_ENV=development

# ============================================
# Backend Konfiguration
# ============================================
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# OCR Engine Konfiguration
# ============================================
# Unterstützte Engines: tesseract, easyocr
OCR_ENGINE=tesseract
# Tesseract Sprachen (Format: eng+deu+fra)
TESSERACT_LANGS=eng+deu

# ============================================
# LLM / AI Konfiguration (optional)
# ============================================
OPENROUTER_API_KEY=

# ============================================
# Qdrant Vector Database (optional)
# ============================================
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# ============================================
# Redis Konfiguration (optional)
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================
# Desktop Client Konfiguration (auf Host)
# ============================================
# Diese werden vom Desktop-Client verwendet, der auf dem HOST läuft
# Nicht innerhalb von Docker!
DESKTOP_CLIENT_ID=desktop_001
DESKTOP_CLIENT_FPS=10
DESKTOP_CLIENT_QUALITY=75
DESKTOP_CLIENT_SCALE=0.8

# WebSocket-Server URL (Backend Container)
# Für Docker Desktop auf Windows/Mac: host.docker.internal
# Für Linux: localhost oder Host-IP
DESKTOP_WS_URL=ws://localhost:8007/ws/live-desktop
DESKTOP_API_URL=http://localhost:8007/api/client