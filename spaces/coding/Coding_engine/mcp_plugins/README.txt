This directory hosts MCP-specific assets (schemas, adapters, and WebSocket client code) for tool call plugins like Travliy Search, Desktop Commander, and Context7.

Current status: scaffold only. No network/WebSocket implementation yet.

Do not place secrets here. Configuration should be managed via config.yaml or environment variables.