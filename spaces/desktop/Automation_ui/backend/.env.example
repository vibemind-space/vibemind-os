# =================================================================
# TRAE Backend Configuration
# Copy this file to .env and adjust values for your environment.
# =================================================================

# Application
ENVIRONMENT=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8007

# Database (PostgreSQL via Docker Compose)
DATABASE_URL=postgresql://trae:change-me-in-production@localhost:5432/automation_ui
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Redis (via Docker Compose)
REDIS_URL=redis://localhost:6379/0

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3003,http://localhost:8007

# Logging
LOG_LEVEL=INFO

# LLM / AI (OpenRouter)
OPENROUTER_API_KEY=sk-or-your-key-here
LLM_MODEL=anthropic/claude-opus-4
VISION_MODEL=nvidia/nemotron-nano-12b-v2-vl:free

# OCR
ENABLE_OCR=true
OCR_LANGUAGES=eng,deu

# Desktop Streaming
ENABLE_DESKTOP_STREAMING=true
DESKTOP_FPS=5
DESKTOP_QUALITY=80

# Remote Execution (set to "remote" for Brain-in-Docker mode)
EXECUTION_MODE=local

# Supabase (optional - for cloud WebSocket relay)
# SUPABASE_WS_URL=wss://your-project-id.supabase.co/functions/v1/live-desktop-stream

# Security (generate a strong secret for production)
# JWT_SECRET_KEY=your-secret-key-at-least-32-characters-long
